package com.joostmsoftware.startingrevamp.item.custom;

import net.minecraft.item.PickaxeItem;
import net.minecraft.item.ToolMaterial;

public class StartingRevampPickaxeItem extends PickaxeItem {
    public StartingRevampPickaxeItem(ToolMaterial material, int attackDamage, float attackSpeed, Settings settings) {
        super(material, attackDamage, attackSpeed, settings);
    }
}
